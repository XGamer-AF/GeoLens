@using Data.ViewModel;
@using DataLayer;
@model UnitsAddViewModel


<a href="@Url.Action("UnitsAddEdit", "Units")" class="btn btn-primary">

    <i class="fas fa-plus-circle"></i>
    @MasterWebApp.Resource.CommonMessage.btnNew
</a>


@{
    string lang = FunServices.getLang("lang");
}

<div style="padding-top:5px;">

    <table id="mTable" class="table table-striped table-bordered table-hover table-responsive">

        <thead>
            <tr>
                <td style="width:40.44%">
                    @MasterWebApp.Resource.Units.unitNameAr
                </td>
                <td style="width:40.44%">
                    @MasterWebApp.Resource.Units.unitNameEn
                </td>
                <td style="width:15.44%">
                </td>


              

                <td style="width:10.1%"></td>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.UnitsAll)
            {

            <tr>
                <td>
                    @item.unitNameAr
                </td>
                <td>
                    @item.unitNameEn
                </td>
               
                <td>
                    <a href="@Url.Action("UnitsAddEdit", new { id = item.unitID })" style="padding:10px;">
                        <i class="far fa-edit"></i>
                    </a>
                </td>

                <td>
                    <a href="@Url.Action("ChangeStatus", new { id = item.unitID, statusID = 0 })" style="padding:10px;" onclick="return confirm('@MasterWebApp.Resource.CommonMessage.confirmDelete')">
                        <i class="fas fa-minus-circle"></i>
                    </a>
                </td>
            </tr>

            }
        </tbody>
    </table>

</div>
